# These are NOT used directly — Google Apps Script has no .env file.
# Add each key as a Script Property in your GAS project:
# Project Settings → Script Properties → Add property

OPENROUTER_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
